# Reading guide

Model = Code in py containing architecture (this is needed to be import when using the pth file)

DIY file = pth file for trained model, (contain all the trained parameter)

results_graph = code + csv results from the training

training_code = code i used for training
